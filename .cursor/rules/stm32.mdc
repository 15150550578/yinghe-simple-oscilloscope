---
description: STM32 单片机 C 开发规范。当生成、审查、调试 STM32（HAL/LL/CMSIS，CubeMX + CMake + VSCode 工具链）代码时应用。仅适用于 STM32，其他 MCU（GD32、51、ESP32、NXP、瑞萨等）不适用本规范。
globs: **/*.c, **/*.h, **/*.s, **/*.ioc, **/CMakeLists.txt, **/*.ld
---

# STM32 单片机 C 开发规范

## 适用范围
仅适用于 STM32（F1/F4/F7/H7/G0/G4/L0/L4 等系列）。其他 MCU 平台（GD32、51、ESP32、NXP、瑞萨、沁恒、复旦微等）不适用本规范。

## 核心硬性约束（必须遵守）
1. 不得凭空编造寄存器地址、位定义、外设特性；不确定时明确告知并请用户提供 STM32 参考手册/芯片头文件。
2. 优先使用芯片官方库（HAL/LL/标准库）与 CMSIS，避免手写不存在的寄存器映射。
3. 所有中断共享变量必须用 `volatile` 声明；访问多字节共享变量需临界区保护。
4. 裸机/RTOS 下默认禁止 `malloc/free` 等动态内存，禁止深递归；确需使用须先向用户确认。
5. 禁止魔法数字，寄存器位、波特率、引脚号等一律用 `#define` 宏或枚举命名。
6. 中断服务函数（ISR）必须短小、快进快出，禁止在 ISR 内调用阻塞/耗时函数；中断回调按 HAL 回调约定实现。
7. 寄存器读-改-写操作注意原子性；涉及多任务/中断的改写需关中断或进临界区。
8. 类型使用 `<stdint.h>` 的 `uint8_t/uint16_t/uint32_t` 等；时钟、外设配置关注 RCC 时钟树与总线频率。
9. 自定义代码只能写在 CubeMX 生成的 `/* USER CODE BEGIN */` 与 `/* USER CODE END */` 标记之间，否则重新生成会被覆盖。
10. 命名、注释、头文件保护、错误码等遵循下方「通用规范」章节。

## 开发工具链规范（CubeMX + VSCode + CMake）
1. 项目骨架由 STM32CubeMX 生成，`.ioc` 文件是唯一配置源头；外设/引脚/时钟的改动一律回到 CubeMX 修改并重新生成，不手改生成代码。
2. 用户自定义逻辑只写在 `/* USER CODE BEGIN xxx */` 与 `/* USER CODE END xxx */` 之间，重新生成前无需迁移。
3. 构建采用 CMake，工具链为 arm-none-eabi-gcc；根目录一个 `CMakeLists.txt`，统一生成 `.elf/.hex/.bin` 产物到 `build/` 目录。
4. 目录约定：`Core/Src`（用户源文件）、`Core/Inc`（头文件）、`Drivers/`（HAL/CMSIS 库）、`Middlewares/`（RTOS/中间件），新增源文件按此归类。
5. 新增/删除源文件后，同步更新 `CMakeLists.txt` 的源码列表；推荐显式列出源文件，避免依赖 GLOB。
6. VSCode 工程配置统一放 `.vscode/`：`settings.json`、`tasks.json`（编译/烧录任务）、`launch.json`（调试）、`c_cpp_properties.json`（IntelliSense 头文件路径与宏定义）。
7. 调试链路：Cortex-Debug 扩展 + ST-Link/J-Link + OpenOCD；断点调试时关注 Cortex-M 内核 Fault 寄存器。
8. 交付涉及工具链的代码时，说明在 CubeMX 中的配置要点，并给出对应的 CMake 片段或 VSCode 配置示例。

## 通用规范（业界通用嵌入式 C 规范）
1. 命名：函数/变量用 snake_case（如 `uart_init`、`rx_buffer`）；宏/常量全大写下划线（如 `UART_BAUD_9600`）；自定义类型用 `_t` 后缀（如 `UartConfig_t`）。
2. 头文件用 `#ifndef / #define / #endif` 防重复包含，宏名形如 `UART_H`。
3. 模块化：每个外设一个 .c/.h；头文件只暴露公共接口，内部函数加 `static`。
4. 注释：关键代码中文注释；函数头说明功能、参数、返回值。
5. 错误码：统一 `int32_t` 返回，0 成功、负值错误码；错误码用枚举定义。
6. 缩进 4 空格，不使用 Tab；单行不超过 120 字符。

## 代码生成标准步骤（SOP）
1. 确认需求：具体 STM32 型号（如 STM32F103C8T6）、库体系（HAL/LL）、时钟频率、所用外设及引脚、RTOS（裸机/FreeRTOS/RTT）。
2. 确认是否基于现有 CubeMX 工程：有 `.ioc` 则读取现有配置，在其上叠加；无则说明所需外设/引脚/时钟，供用户在 CubeMX 中配置。
3. 确认芯片头文件与库来源，未提供时主动索取型号与参考手册。
4. 按模块分层生成：时钟与 GPIO 初始化 → 外设初始化 → 读写/中断接口 → 应用调用示例。
5. 每个外设驱动固定输出：头文件声明、源文件实现、关键寄存器/参数中文注释。
6. 涉及中断时，明确 NVIC 优先级分组、使能，并给出对应中断回调/服务函数。
7. 为硬件等待、超时逻辑增加超时保护与错误返回。
8. 新增源文件时说明需同步到 CMakeLists.txt 的位置；生成后附一段最简可编译的调用示例。

## 代码审查检查清单（SOP）
1. 是否所有硬件寄存器/中断共享变量已加 volatile；
2. 是否存在裸指针越界、数组越界、未初始化变量；
3. 中断上下文是否调用了不可重入/阻塞函数；HAL 回调是否在中断内做过重操作；
4. 是否存在未处理的错误返回、死循环无超时；
5. 位操作是否正确（移位方向、掩码、清位/置位）；
6. NVIC 优先级、临界区、时钟配置是否合理；
7. 动态内存、递归、栈溢出风险；
8. 自定义代码是否放在 USER CODE 区间内、是否会被 CubeMX 重新生成覆盖；
9. CMakeLists 是否包含新增源文件、头文件路径与宏是否齐全；
10. 命名、宏、头文件保护是否符合通用规范。

## 调试分析标准步骤（SOP）
1. 收集现场信息：STM32 型号、现象（死机/复位/外设异常）、复现条件、日志/寄存器/PC 指针、调试器（ST-Link/J-Link + OpenOCD）。
2. 判断故障域：硬件电气 → 电源/时钟（RCC）→ 外设配置 → 中断 → 内存/栈 → 逻辑。
3. 针对 HardFault：读取 Cortex-M 内核 Fault 寄存器（CFSR、HFSR、BFAR、MMFAR）与栈中 PC/LR，定位越界、空指针、除零、非法访问、栈溢出、优先级配置错误。
4. 针对复位：读 RCC_CSR 复位原因寄存器、看门狗标志辅助定位。
5. 结合 VSCode + Cortex-Debug 断点/单步定位到具体源码行。
6. 输出结论时给出：最可能原因、排查步骤、修改建议，并说明依据。

## 输出格式
- 代码：完整可编译的 .c/.h，关键处中文注释说明寄存器含义与设计意图。
- 审查：按清单逐条给出「问题 / 严重程度 / 位置 / 修改建议」。
- 调试：按「结论 / 依据 / 排查步骤 / 修复建议」结构化输出。
- 工程配置：涉及 CubeMX/CMake/VSCode 时，给出具体配置说明或配置片段。

## 交付自查清单
1. 是否基于真实 STM32 型号/头文件，无编造；
2. 是否满足上述硬性约束与通用规范；
3. 是否遵循 CubeMX + CMake + VSCode 工具链规范；
4. 代码能否通过编译（类型、宏、头文件、CMake 源码列表完整）；
5. 是否附调用示例与关键注释。
